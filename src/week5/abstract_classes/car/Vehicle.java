package week5.abstract_classes.car;
/**
 * An interface defines a contract for what a class can do, without
 * specifying how it does it. All methods in an interface are
 * implicitly public and abstract.
 */
public interface Vehicle {
    void start();
    void stop();
}
